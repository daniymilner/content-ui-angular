.spacer {
  flex: 1 auto;
}

.title {
  margin-left: 8px;
  font-weight: 300;
  letter-spacing: 1px;
}

mat-icon {
  font-size: 18px;
  text-align: center;
  padding-top: 6px;
  width: 32px;
}

mat-icon.as-button {
  cursor: pointer;
}

mat-toolbar {
  height: 32px;
  min-height: 32px;
  padding: 0 12px;
  font-size: 18px;
}

mat-toolbar-row {
  padding: 0;
}
